apiVersion: v1
kind: Secret
metadata:
  name: openai-secrets
  namespace: poc-onprem
type: Opaque
stringData:
  # IMPORTANT: Replace REPLACE_WITH_YOUR_OPENAI_API_KEY with your actual API key
  # DO NOT commit real API keys to version control
  OPENAI_API_KEY: "REPLACE_WITH_YOUR_OPENAI_API_KEY"

# Made with Bob
