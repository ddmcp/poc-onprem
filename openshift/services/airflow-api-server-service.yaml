apiVersion: v1
kind: Service
metadata:
  name: airflow-api-server
  namespace: poc-onprem
  labels:
    app: airflow-api-server
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: airflow-api-server

# Made with Bob
