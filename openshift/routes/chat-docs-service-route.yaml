apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: chat-docs-service
  namespace: poc-onprem
  labels:
    app: chat-docs-service
spec:
  to:
    kind: Service
    name: chat-docs-service
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None

# Made with Bob